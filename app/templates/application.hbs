{{page-title "Booklist"}}


<div class="bg-blue-900 text-gray-100 p-8 h-screen">
    <div class="mx-auto mb-4">
        <div class="h-12 flex items-center mb-4 border-b-2">
            <a href="#">
                <h1 class="font-bold text-2xl">
                    booklist. 
                </h1>
            </a>
        </div>
        {{!-- Add a navigation link --}}
        <nav class="h-8 flex items-center mb-8">
            <LinkTo class="hover:text-gray-500" @route="authors">Authors</LinkTo>
            <LinkTo class="hover:text-gray-500 ml-4" @route="books" >Books</LinkTo>
        </nav>

            {{outlet}}
    </div>
</div>




